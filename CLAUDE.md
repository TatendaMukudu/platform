# Conventions

- **No emojis.** Not in the UI, not in code, not in commit messages, not in docs. Use plain
  text labels and, where an icon is genuinely needed, an inline SVG (as the composer send/attach
  controls already do).
