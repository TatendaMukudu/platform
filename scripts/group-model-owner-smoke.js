'use strict'; process.env.DB_OPTIONAL='1';
const S=require('../server'); let p=0,f=0; const ok=(n,c)=>{if(c){p++;console.log('  PASS',n)}else{f++;console.error('  FAIL',n)}};
const C='group-owner'; S._loadAllStores({orgMeta:{[C]:{orgName:'Owner'}},orgUsers:{[C]:{a:{id:'a',name:'Admin',email:'a@g.test',role:'superadmin',orgCode:C,status:'active'}}},orgGroups:{[C]:[{id:'legacy',name:'Legacy',memberIds:['a'],leadIds:['a']}]}}); S._rebuildEmailIndex();
ok('F54.1 the load boundary migrates legacy groups into the canonical orgNodes model',S.orgNodes[C]&&S.orgNodes[C].legacy&&S.orgNodes[C].legacy.leaderIds[0]==='a');
const server=S.app.listen(0,async()=>{try{const base=`http://127.0.0.1:${server.address().port}`,token=S.issueToken('a',C,'superadmin');const r=await fetch(base+'/api/groups/create',{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({name:'Canonical',memberIds:['a'],leadIds:['a']})});const j=await r.json();ok('F54.2 the existing group HTTP boundary writes orgNodes directly',r.status===200&&S.orgNodes[C][j.group.id].name==='Canonical'&&S.orgNodes[C][j.group.id].leaderIds[0]==='a');}catch(e){console.error(e);f++}server.close(()=>{console.log(`\ngroup-model-owner-smoke: ${p} passed, ${f} failed`);process.exit(f?1:0)})});
